# LOGICAL GATE STANDARDS
   QALU has all classical logic gates(i.e and, or, xor, xnor). AND gate is named as **and_**() instead of **and()** in order to prevent confliction. Otherwise, python core interpreter would interpreter it as quantum logic gate instead of classical and logical operation
## AND 
![](src/and.png)
   And gate is used to perform and operation
